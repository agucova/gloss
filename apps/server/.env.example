# Database (PlanetScale Postgres - get from dashboard "Connect" tab)
# Use port 6432 for PgBouncer (connection pooling, recommended)
# Use port 5432 for direct connections (limited by max_connections)
DATABASE_URL=postgresql://username:password@host.psdb.cloud:6432/postgres?sslmode=verify-full

# Authentication
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-long
BETTER_AUTH_URL=http://localhost:3000

# CORS (URL of your web app)
CORS_ORIGIN=http://localhost:3001

# Server (Railway injects PORT automatically)
NODE_ENV=development
PORT=3000
