# Enable devenv integration with direnv
# See https://devenv.sh/automatic-shell-activation/

if ! has devenv; then
  echo "devenv not found. Install it from https://devenv.sh/getting-started/"
  exit 1
fi

# Use devenv
eval "$(devenv print-dev-env)"
